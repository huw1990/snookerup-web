import classes from './Slider.module.css';

const Slider = (props) => {
  return (
    <h1 className={classes.slider}>
      Want to level up your<span></span>
    </h1>
  );
};

export default Slider;